# Contrast And Motion Review

Check:

- contrast of text and interactive states
- non-color signal for status
- visible focus indicator strength
- hover-only information loss
- animation essentiality
- reduced-motion fallback

Block release when:

- task completion depends on color alone
- focus is invisible or easy to lose
- motion is necessary to understand the UI and no fallback exists
