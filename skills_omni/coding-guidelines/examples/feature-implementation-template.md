# Feature Implementation Prompt Template

```text
Use @coding-guidelines to implement <feature> in <path or module>. Confirm acceptance criteria first, inspect similar patterns already in the repository, make the smallest implementation that satisfies the request, update or add focused tests, and summarize changed files, validation results, assumptions, and residual risks.
```
