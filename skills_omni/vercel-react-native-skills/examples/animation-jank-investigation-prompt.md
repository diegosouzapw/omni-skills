# Animation Jank Investigation Prompt

```text
Use @vercel-react-native-skills to investigate animation jank in a gesture-heavy screen. Separate JS-thread work from UI-thread animation concerns, verify Reanimated and Gesture Handler setup, prefer transform/opacity targets where possible, and state what must be validated in release mode.
```
