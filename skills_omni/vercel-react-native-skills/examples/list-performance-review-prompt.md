# List Performance Review Prompt

```text
Use @vercel-react-native-skills to review a large React Native or Expo list. Start with environment triage, confirm whether the issue reproduces in release mode, inspect row memoization and callback identity, review image cost inside rows, tune FlatList before suggesting FlashList, and end with the minimum justified changes.
```
