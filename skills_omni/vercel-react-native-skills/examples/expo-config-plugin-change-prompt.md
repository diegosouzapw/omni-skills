# Expo Config Plugin Change Prompt

```text
Use @vercel-react-native-skills to review an Expo change involving fonts, app config, or a native module. Determine whether Expo Go is sufficient, check config-plugin requirements, identify whether prebuild/regeneration is needed, and propose only the narrow next steps required to validate the change.
```
