# New API Service Style Selection Example

```text
Use @aspnet-core to choose between Minimal APIs and controllers for a new service in this repository. Base the recommendation on the existing codebase style, API surface area, validation needs, and maintainability, then list the first implementation steps.
```
