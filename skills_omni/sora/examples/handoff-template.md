# Sora Handoff Template

Use this when handing work to a reviewer, another agent, or another skill.

## Task summary

- User goal:
- Workflow chosen: `create` / `edit` / `extend` / `create-character` / `poll` / `download` / local `create-batch` / official Batch API
- Reason this path was chosen:

## Provenance

- Skill: `@sora`
- Source repository or import note:
- Relevant references used:

## Inputs

- Prompt file path:
- Prompt summary:
- Model:
- Size:
- Seconds:
- Character IDs, if any:
- Source video or image inputs, if any:

## Execution record

- Command(s) run:
- Job or video ID(s):
- Poll status reached:
- Output path(s):

## Results

- What succeeded:
- What failed or remains uncertain:
- Any temporary URLs already converted to local files:

## Risks or follow-up

- Guardrail or policy considerations:
- Open technical questions:
- Recommended next step:

## If handing off to another skill

Preserve the prompt, parameters, IDs, output paths, and provenance so the next skill can continue without re-discovery.
